<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Add Internet permission (required for Firebase authentication) -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:label="ride_mate"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme"/>

            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Required for Google Sign-In -->
        <meta-data
            android:name="com.google.android.gms.auth.api.signin"
            android:value="true"/>

        <!-- OAuth client ID metadata (replace with actual value from google-services.json) -->
        <meta-data
            android:name="com.google.android.gms.auth.api.signin.OAuthClientId"
            android:value="@string/default_web_client_id"/>

        <!-- Don't delete this meta-data; it’s used by Flutter -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2"/>

    </application>

    <!-- Queries for text-processing -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
